services:
  flippy:
    build: .
    image: flippy
    ports:
      - "8080:8080"
    env_file:
      - .env            # OPENROUTER_KEY=..., GROQ_KEY=... etc (see .env.example)
    environment:
      - HOST=0.0.0.0
      - PORT=8080
    restart: unless-stopped
